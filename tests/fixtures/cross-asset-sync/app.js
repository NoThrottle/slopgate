const root = document.getElementById("app");
const list = document.getElementsByClassName("shell");
const one = document.querySelector(".shell");
const two = document.querySelectorAll("#app");

console.log(root, list, one, two);
